# Baseline requirements for the Victoria Urban Planning - Urban Streetscape Intervention Analysis Project
# Data Processing & Analysis
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
statsmodels>=0.14.0

# Visualization
matplotlib>=3.7.0
plotly>=5.14.0
seaborn>=0.12.0

# Geospatial Analysis
geopandas>=1.0.0
shapely>=2.0.0
pyproj>=3.5.0
# fiona removed: GeoPandas 1.0+ defaults to the pyogrio I/O engine and no longer
# installs fiona. Fiona has no cp314 wheel, so pip falls back to a source build
# that requires GDAL headers and fails on Windows. Nothing in this repo imports it.
# If you need it explicitly, use pyogrio instead (fiona.listlayers -> pyogrio.list_layers).

# Development Environment
jupyterlab>=4.0.0

# API Layer & Backend
fastapi>=0.100.0
uvicorn>=0.22.0
duckdb>=0.8.0
requests>=2.31.0
pyyaml>=6.0.0
